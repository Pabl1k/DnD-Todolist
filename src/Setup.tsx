import MainPage from "./containers/MainPage/MainPage";
import "./Setup.scss";

const Setup = () => {
  return (
    <div className="app-setup">
      <MainPage />
    </div>
  );
};

export default Setup;
